工作日志：基于 OpenHarmony 的 UART 通信协议实现与小车控制


项目名称： 12.0_UART_Correspondence (OpenHarmony WiFi IoT)
任务阶段： 阶段三 - 系统通信协议与控制

1. 任务概述
本次任务主要完成了 OpenHarmony（Hi3861）与 STM32 之间的 UART 串口通信功能。通过自定义通信协议，实现了上位机（WiFi IoT）向底层（STM32）发送电机控制指令的功能，包括小车的前进、后退及速度调节。

2. 具体工作内容

2.1 工程搭建与环境配置
- 工程创建：复制 1.0_Hello_World 工程并重命名为 12.0_UART_Correspondence。
- 文件重构：将原有的 C 文件重命名为 correspondence.c 以匹配业务逻辑。
- 构建配置：修改 BUILD.gn 文件，配置 lite_component，引入 12.0_UART_Correspondence:correspondence 依赖，确保编译路径正确。

2.2 通信协议设计
设计了包含帧头、数据位和帧尾的 6 字节定长数据帧，以确保数据传输的准确性：
- 帧头 (Byte0)：0xFC，用于标识数据帧开始。
- 左轮方向 (Byte1)：0 代表正转（前进），1 代表反转（后退）。
- 左轮速度 (Byte3)：范围 -150 ~ 150，精度 0.01，单位转/每秒。
- 右轮方向 (Byte4)：0 代表正转，1 代表反转。
- 右轮速度 (Byte5)：范围 -150 ~ 150。
- 帧尾 (Byte6)：0xFD，用于标识数据帧结束。

2.3 核心代码实现 (correspondence.c)
- UART 初始化：配置串口参数为波特率 115200，8位数据位，1位停止位，无校验 (WifiIotUartAttribute)。
- 电机控制逻辑 (stm32motor_control)：
    - 实现了输入参数 motorA 和 motorB 的正负判断逻辑，自动转换方向标志位 (A_dir, B_dir) 并取绝对值。
    - 增加了速度限幅保护，将速度限制在 -150 到 150 之间。
    - 按照协议组装 uart_sendbuf 数组并调用 UartWrite 发送。
- 多线程管理：使用 osThreadNew 创建了 thread1 和 thread2，并配置了优先级（Priority 25）和栈大小（1024*4）。

2.4 接收端解析逻辑 (STM32侧配合)
- 编写了中断服务程序 USART1_IRQHandler，通过检测帧头 0xFC 和帧尾 0xFD 来锁定有效数据包。
- 实现了数据还原算法：接收到的速度值除以 100.0 得到实际转速，并根据方向位设置正负号，最终调用 PID 控制函数 CalculateAndControlMotors。

3. 遇到的问题与解决方案
- 问题：初始编译时找不到头文件路径。
- 解决：在 BUILD.gn 的 include_dirs 中补充了 "//base/iot_hardware/interfaces/kits/wifiiot_lite" 等必要路径。
- 问题：电机反转逻辑混乱。
- 解决：在发送端统一处理符号位，将负数速度转换为正数绝对值加上方向标志位 1，简化了接收端的解析难度。

