# STM32 智能小车电机驱动调试工作日志

**项目名称**：QST先锋号小车电机控制实验  
**硬件平台**：STM32F103 微控制器 + L9110S 电机驱动模块  
**开发环境**：Keil MDK  
**调试日期**：2026-08-26  

---

## 一、 问题背景与初始状态
在 QST 小车电机控制实验中，由于工程文件中缺失 `motor.c` 驱动文件，导致电机无法进行初始化和速度控制。需手动编写电机驱动代码，并完成硬件引脚的匹配与调试。

## 二、 调试过程与问题解决

### 1. 底层驱动代码编写
- **操作**：根据实验要求，新建 `motor.c` 与 `motor.h` 文件，编写了包含 GPIO 初始化、定时器配置及占空比设置的底层驱动代码。
- **问题**：编译时出现 `undefined symbol` 报错。
- **解决**：确认 `motor.c` 未正确添加至 Keil 工程的文件树中。通过右键 Group 添加文件并重新全量编译（Rebuild），解决链接错误。

### 2. 主程序逻辑与语法修复
- **问题**：代码烧录后小车无反应，且编译报错。
- **排查**：
  - 发现 `main.c` 中核心初始化代码被意外包裹在 `/* ... */` 注释块中，导致程序无入口。
  - 发现 `delay_init(72)` 参数数量不匹配。
- **解决**：取消注释并补全 `Motor_Init()` 与 `PWM_Init()` 调用；将 `delay_init(72)` 修正为无参调用 `delay_init()`。

### 3. 硬件引脚冲突与高级定时器配置
- **问题**：代码逻辑无误但小车仍不动。
- **排查**：通过查阅硬件原理图，发现引脚配置与常规代码不符。
  - 左电机 PWM 引脚为 PB7（TIM4 CH2），方向引脚为 PB14。
  - 右电机 PWM 引脚为 PB13（TIM1 CH1N），方向引脚为 PB6。
- **解决**：
  - 释放 JTAG 引脚：在 `main.c` 中调用 `JTAG_Set(SWD_ENABLE)`，释放被占用的 PB13 引脚。
  - 引入高级定时器：针对右电机所在的 TIM1 高级定时器，在代码中补充了 `TIM_RepetitionCounter` 配置及关键的 `TIM_CtrlPWMOutputs(TIM1, ENABLE)` 使能主输出函数。

### 4. 最终故障定位
- **现象**：Keil 显示 `Programming Done. Verify OK.`，代码成功烧录，但电机依旧无动作。
- **解决**：经现场排查，发现小车底板上的**电机独立供电开关未打开**。打开电源后，电机成功响应 PWM 信号运转，问题彻底解决。

## 三、 调试总结与经验沉淀
1. **先查硬件，后查软件**：嵌入式开发中，单片机与电机通常采用独立供电。当单片机正常（如 LED 亮、串口有输出）而电机不动时，应优先检查电机供电电压、开关状态及接线是否牢固。
2. **严谨核对原理图**：切勿盲目套用网络代码。STM32 的引脚复用功能复杂（如 PB13 默认被 JTAG 占用），编写驱动前必须严格对照原理图，确认引脚对应的定时器通道（特别是高级定时器的互补通道配置）。
3. **规范工程习惯**：新建外设驱动后，务必第一时间将 `.c` 文件加入工程，并确保头文件路径配置正确，避免低级编译错误。

## 四、 后续工作计划
- 封装基础运动控制函数（如 `go_forward()`, `turn_left()`），为后续巡线或避障算法提供底层接口。
- 测试不同占空比下的电机响应曲线，确定克服静摩擦力所需的最小启动速度。