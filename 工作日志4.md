# STM32 智能小车底层驱动与闭环控制系统开发工作日志

**项目阶段**：底层驱动编写与闭环控制调试  
**硬件平台**：STM32F10x 系列微控制器、双轮编码器电机、TB6612/L298N 驱动模块  
**开发工具**：Keil uVision、串口调试助手  

---

## 一、 工作进展与核心任务

### 1. 底层驱动架构重构（无库化开发）
- **任务描述**：在缺失 `control_system.h` 官方库的情况下，手动重构并补全了小车底层的控制逻辑。
- **具体实施**：
  - 新建并完善了 `encoder.h` / `encoder.c`，基于 TIM2 和 TIM3 配置编码器模式，实现了 `Read_Encoder` 函数以读取脉冲数。
  - 新建了 `control_system.h` / `control_system.c`，将 `SysTick_Handler` 中断服务函数与 `System_Control` 业务逻辑进行整合，实现了基于 100ms 周期的定时任务调度。
  - 解决了 `stm32f10x_it.c` 与 `control_system.c` 中 `SysTick_Handler` 重复定义导致的链接错误（`Error: L6200E`），统一了中断入口。

### 2. 速度闭环控制系统（PID）实现
- **任务描述**：在主函数中引入速度反馈机制，实现增量式 PID 闭环控制。
- **具体实施**：
  - 编写了 `Incremental_PI_A`（左轮）和 `Incremental_PI_B`（右轮）增量式 PI 控制器，包含误差计算、积分限幅及 PWM 输出逻辑。
  - 梳理了主函数（`main.c`）架构，确立了“中断驱动后台控制 + 主循环设定目标速度”的分离式架构。
  - 增加了全局目标速度变量（`Target_L_Speed`, `Target_R_Speed`），实现了控制指令与底层执行的解耦。

### 3. 系统调试与异常排查
- **串口反馈集成**：在 `System_Control` 中集成了 `printf` 格式化输出，重定向了 `fputc` 函数，实现了串口助手对 `L_coder`、`R_coder` 及目标速度的实时数据监控。
- **右轮运动异常排查**：
  - **现象**：右轮出现严重的“一下一下”顿挫感与转速不稳定。
  - **软件调参尝试**：进行了多轮 PID 参数整定。先后尝试了降低比例系数（Kp）、归零微分系数（Kd）、关闭积分系数（Ki）以及增大 Kp 等操作。
  - **硬件故障定位**：在软件调参无效且现象加剧后，将问题排查方向转向硬件。分析了编码器内部死区/卡顿、联轴器松动、机械摩擦不均以及电源压降等潜在硬件故障，并制定了开环测试与交叉验证的排查方案。

---

## 二、 遗留问题与下一步计划

### 1. 硬件交叉验证（高优先级）
- 执行右轮开环盲测（直接给定固定 PWM），确认右轮顿挫是否由电机本体或机械结构引起。
- 观察串口打印的 `R_coder` 数据，排查是否存在脉冲丢失或跳变现象。
- 视情况对调左右轮电机或编码器接线，彻底锁定故障硬件。

### 2. 控制参数精调
- 在确认硬件无异常后，重新进行 PID 参数整定。
- 采用“先 P 后 I”的策略，逐步增加比例系数使系统响应连贯，最后引入微小的积分系数消除稳态误差。

### 3. 功能扩展
- 完善串口通信协议，支持通过串口指令动态修改左右轮目标速度。
- 增加系统状态指示（如通过 LED 闪烁频率指示系统运行状态）。

---

## 三、 工作心得
本次开发从底层驱动手写起步，完整经历了“架构搭建 -> 算法实现 -> 软硬联调 -> 故障排查”的全流程。在右轮顿挫问题的排查中，深刻认识到嵌入式控制系统的“木桶效应”。