工作日志：任务26 综合系统实验-蓝牙遥控小车

任务名称：综合系统实验-蓝牙遥控小车  

一、任务概述
本任务为小车系统三大功能之一——蓝牙遥控模式的实现。通过蓝牙通信实现短距离、高频次的指令传输，控制小车完成前进、后退、左转、右转及停止等动作。重点与难点均为蓝牙遥控逻辑的实现与串口通信协议的调试。

二、任务执行步骤

1. 工程创建与文件重命名
   - 复制 1.0_Hello_World 工程至 app 目录下，重命名为 14.0_Bluetooth_control。
   - 将原工程中的C文件重命名为 Control.c，作为主控逻辑文件。

2. 代码编写与结构搭建
   - 头文件引入：在 Control.c 中添加必要头文件，包括 stdio.h, stdlib.h, unistd.h, memory.h, wifiiot_uart.h, ohos_init.h, cmsis_os2.h, wifiiot_gpio.h, hi_io.h, hi_time.h, wifiiot_pwm.h, hi_pwm.h, hi_uart.h, wifiiot_gpio_ex.h, hi_task.h, wifiiot_errno.h。
   - 变量定义：定义全局变量 uart_sendbuf[20] 用于发送数据，bluetooth_flag[1000] 用于接收蓝牙标志位。
   - 核心函数实现：
     - stm32motor_control(int motorA, int motorB)：封装电机控制逻辑，支持方向判断（正转/反转）、速度限幅（-150~150），并按协议打包数据（帧头0xFC，帧尾0xFD）通过UART2发送至STM32。
     - car_forward(), car_backward(), car_left(), car_right(), car_stop()：分别调用 stm32motor_control 实现小车基本动作。
     - car_mode_bluetooth()：蓝牙模式主循环，持续读取UART1接收的蓝牙指令（如 'W','A','S','D','I','K'），并调用对应动作函数。
     - Control()：主任务函数，初始化GPIO、UART1（蓝牙）、UART2（电机控制），创建蓝牙模式线程。

3. 系统配置修改
   - 修改 app_main.c 中 APP_INIT_EVENT_NUM 为 7，以支持同时使用串口1和串口2。
   - 修改 BUILD.gn 文件，添加 14.0_Bluetooth_control:Control 到 features 列表，确保编译包含本模块。

4. 编译与烧录
   - 在终端执行 python build.py wifiiot，编译成功生成 wifiiot_app_ota.bin。
   - 烧录至开发板，连接JYD-16蓝牙模块，使用配套APP或串口助手发送指令测试。

三、关键代码片段（可直接复制）

// Control.c 核心函数示例
void stm32motor_control(int motorA, int motorB) {
    uint8_t A_dir = 0, B_dir = 0;
    if (motorA < 0) { A_dir = 1; motorA = -motorA; }
    if (motorB < 0) { B_dir = 1; motorB = -motorB; }
    if (motorA > 150) motorA = 150;
    if (motorB > 150) motorB = 150;

    uart_sendbuf[0] = 0xFC; // 帧头
    uart_sendbuf[1] = A_dir;
    uart_sendbuf[2] = motorA;
    uart_sendbuf[3] = B_dir;
    uart_sendbuf[4] = motorB;
    uart_sendbuf[5] = 0xFD; // 帧尾
    UartWrite(WIFI_IOT_UART_IDX_2, (unsigned char *)uart_sendbuf, 6);
}

static void car_mode_bluetooth(void) {
    while(1) {
        UartRead(WIFI_IOT_UART_IDX_1, bluetooth_flag, 1000);
        if(bluetooth_flag[0] != 0) {
            switch(bluetooth_flag[0]) {
                case 'W': car_forward(); break;
                case 'S': car_backward(); break;
                case 'A': car_left(); break;
                case 'D': car_right(); break;
                case 'I': stm32motor_control(100, 100); break; // 高速前进
                case 'K': stm32motor_control(150, 150); break; // 极速前进
                default: car_stop(); break;
            }
            bluetooth_flag[0] = 0; // 清空缓冲
        }
        hi_sleep(50);
    }
}

四、注意事项
- 小车通过数据线连接电脑时可直接供电，但运行电机功能时需使用电池供电。
- STM32部分代码复用串口通信章节内容，确保UART2波特率、数据位等参数与STM32端一致。
- 蓝牙指令需与APP或串口助手发送字符严格匹配（如 'W' 代表前进）。

五、后续优化建议
- 增加速度调节功能（如通过 'I'/'K' 切换不同速度档位）。
- 添加异常处理机制（如接收超时自动停车）。
- 扩展多模式切换（如蓝牙/红外/自动避障模式）。

✅ 任务已完成，代码已编译通过，功能验证正常。
