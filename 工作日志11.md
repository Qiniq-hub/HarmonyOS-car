# STM32 串口收发打印实验工作日志

**项目名称：** QST先锋号小车串口通信实验


---

## 一、 环境搭建与基础验证

1. **硬件连接：** 使用 USB-TypeC 线连接小车与电脑，将主板串口切换开关拨至 **STM32** 端。
2. **驱动配置：** 安装 CH340 驱动，确认端口号（如 COM5），XCOM 助手波特率设为 **115200**。
3. **样例验证：** 烧录模板工程，复位小车收到 `QST青软`，发送 `HELLO` 跑马灯闪烁，基础链路正常。

---

## 二、 任务完成情况

###  任务 1：定时发送十六进制数组
- **内容：** 定义数组 `{0x01, 0x02, 0x03, 0x04, 0x05}`，在 `while(1)` 中循环发送并延时 1 秒。
- **结果：** 电脑端勾选“16进制显示”后，稳定接收到 `01 02 03 04 05`。

###  任务 2：接收 HEX 指令控制跑马灯
- **内容：** 修改中断服务函数，将判断条件从字符 `'H'` 改为十六进制 `0x01`，并校验接收长度是否为 5。
- **结果：** 电脑端勾选“16进制发送”发送 `01 02 03 04 05`，小车成功识别并点亮跑马灯。

###  任务 3：综合调试与验证
- **内容：** 整合上述功能，确保收发互不干扰。
- **结果：** 实现了双向十六进制通信，数据解析准确，响应及时。

---


## 三、 实验总结

通过本次实验，成功实现了 STM32 串口的双向通信。重点掌握了以下技能：
1. **数据格式区分：** 深刻理解了 ASCII 字符（如 '1'）与十六进制数值（如 0x01）在底层传输的区别。
2. **协议解析：** 学会了通过“帧头+长度”的方式（如判断首字节 0x01 和长度 5）来解析不定长数据流。
3. **调试技巧：** 熟练使用串口助手进行 Hex 收发的排查与验证。