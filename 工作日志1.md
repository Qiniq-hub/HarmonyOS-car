# STM32 小车串口通信实验工作日志

## 1. 实验目标
通过 MDK 和 ST-Link 将控制程序烧录至 STM32 小车，实现通过电脑串口助手发送指令，控制小车 LED 灯进行跑马灯效果
## 2. 核心代码逻辑
在 `while(1)` 主循环中，通过判断串口接收标志位 `USART_RX_STA` 来触发灯效：
1. 当接收到完整数据时，调用 `L_runingled()` 点亮 LED。
2. 将 `USART_RX_STA` 标志位清零，等待下一次指令。

## 3. 遇到的问题与解决
- **问题 1**：灯收到指令后一直狂闪或常亮。
  **解决**：发现是处理完指令后没有将 `USART_RX_STA` 标志位清零，导致程序无限循环执行点灯操作。在代码中补充了 `USART_RX_STA = 0;` 后解决。
- **问题 2**：串口助手发送数据无反应。
  **解决**：检查发现波特率不匹配，将串口助手波特率统一设置为 115200 后通信恢复正常。

## 4. 实验总结
本次实验成功实现了 STM32 与上位机的串口双向通信。