# 工作日志
**日期**：2026年8月30日  
**任务主题**：OpenHarmony系统驱动实验 - SHT20温湿度采集与信号量同步



---



## 今日完成工作
1. 完成任务12的全流程学习与实操，掌握SHT20温湿度传感器驱动开发、LiteOS-M信号量机制与多线程同步的实现方法。
2. 完成工程创建与目录部署：复制1.0_Hello_World工程到app目录下，重命名为`8.0_Sht20`，将核心C文件重命名为`Sht20.c`，导入supportPack中2_sht20的include、src驱动文件夹完成环境搭建。
3. 梳理核心原理：
   - 学习SHT20温湿度传感器的硬件特性，其测量精度可达±3%RH、±0.3℃，采用IIC数字接口，使用寿命可达15年
   - 掌握信号量（Semaphore）的核心概念，理解其作为任务间通信机制，实现多任务同步、共享资源互斥访问的工作逻辑
   - 理清SHT20与Hi-3861模块的硬件接线关系，确认IIC_SDA、IIC_SCL引脚的连接规范
4. 完成全流程代码编写：
   - 导入标准库、OpenHarmony内核与SHT20驱动相关头文件
   - 定义信号量全局变量，编写3个独立线程的创建逻辑，配置统一栈大小与优先级
   - 初始化最大计数为4的信号量sem1，实现线程间同步调度
   - 编写3个线程的业务逻辑：Thread1负责定时释放两次信号量，Thread2获取信号量后读取SHT20温湿度数据并打印，Thread3获取信号量后输出同步提示信息
5. 完成编译配置：
   - 编写`8.0_Sht20`目录下的BUILD.gn，配置静态库的源文件路径与头文件搜索目录
   - 修改APP根目录的BUILD.gn，将Sht20组件加入全局编译特性列表
6. 执行编译烧录与结果验证：
   - 在终端执行`python build.py wifiiot`命令，得到BUILD SUCCESS完整编译结果
   - 按照规范将串口开关拨到3861端，通过数据线供电完成烧录，关闭小车自带供电开关
   - 按下开发板复位键后系统正常运行，串口调试助手持续输出实时温湿度数值，同时打印3个线程的信号量获取、释放同步日志，多任务调度逻辑运行正常。



---



## 关键知识点收获
- SHT20通过IIC接口完成数据交互，无需额外AD采样即可直接输出校准后的温湿度数值，大幅降低嵌入式开发工作量
- OpenHarmony LiteOS-M中通过`osSemaphoreNew`、`osSemaphoreAcquire`、`osSemaphoreRelease`接口可以快速实现信号量创建、等待与释放，灵活控制多任务执行顺序
- 计数型信号量可以同时支持多个任务获取资源，通过调整信号量释放次数可以实现多线程并行同步或交替运行的不同效果



---



## 后续计划
正式进入阶段三的STM32开发学习，从基础外设驱动开始逐步完成后续串口、PWM等实验任务。
