# OpenHarmony (Hi3861) SG90 舵机控制与系统烧录调试工作日志

**项目名称**：基于 OpenHarmony 的 SG90 舵机互斥锁控制实验
**硬件平台**：Hi3861 WiFi IoT 开发板 + SG90 舵机
**开发环境**：VS Code + DevEco Device Tool / HiBurn
**调试日期**：2026-08-27

---

## 一、 问题背景与初始状态
在基于 OpenHarmony 的 SG90 舵机控制实验中，需编写多线程控制逻辑（利用互斥锁实现舵机角度切换），并完成代码编译与固件烧录。但在构建与烧录过程中，连续遭遇头文件路径缺失、API 版本不匹配及固件启动失败等问题。

## 二、 调试过程与问题解决

### 1. 构建系统路径报错 (`BUILD.gn` 配置)
- **问题**：编译时报错 `fatal error: wifi_iot_gpio.h: No such file or directory`。
- **排查**：虽然代码引用了 `wifi_iot_gpio.h`，但构建脚本未声明该头文件的搜索路径。
- **解决**：修改组件目录下的 `BUILD.gn` 文件，在 `include_dirs` 中显式添加 `//base/iot_hardware/interfaces/kits/wifiiot_lite` 路径。

### 2. Hi3861 固件烧录失败
- **问题**：使用 HiBurn 工具烧录时，日志反复提示 `Ready to load at 0x10A000` 及 `CCCCCCCCload fail`。
- **排查**：小车未复位
- **解决**：
  - 采用“按住 Reset 键 -> 插入 USB -> 松开 Reset 键”的物理操作，强制芯片进入下载模式，成功完成烧录。

## 三、 调试总结与经验沉淀
1. **OpenHarmony 构建规范**：HarmonyOS 源码目录层级深，新增外设驱动或模块时，必须同步修改 `BUILD.gn` 中的 `include_dirs` 和 `deps`，切忌仅依赖代码中的相对路径。
2. **Hi3861 烧录机制**：Hi3861 对烧录时序要求严格。遇到 `load fail` 或 `hupg fail` 时，优先排查物理复位时序、波特率设置及固件文件的完整性，避免在代码逻辑上浪费时间。

## 四、 后续工作计划
- 验证重刷固件后系统是否正常启动。
- 测试 SG90 舵机在不同角度（0°、90°、180°）下的响应情况，验证互斥锁机制是否生效。
