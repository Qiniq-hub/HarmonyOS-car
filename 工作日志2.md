# 工作日志：OpenHarmony (Hi3861) 开发环境配置与编译

**日期**：2026年8月26日
**事项**：鸿蒙轻量级系统（Hi3861）开发板环境搭建、编译及烧录准备

---

### 今日工作内容

1. **梳理开发板全流程操作指南**
   - 明确了 OpenHarmony 轻量系统（Hi3861）的“代码编译”与“固件烧录”两大核心阶段。
   - 整理了硬件连接、拨码开关设置（SW2拨至3861）、HiBurn工具配置（波特率2000000、勾选Auto burn）及烧录验证的完整SOP。


2. **解决产品未找到的编译指令错误**
   - **问题现象**：直接运行 `python build.py` 时，终端提示 `Error: product not found.`。
   - **解决方案**：明确编译指令需附带产品代号，针对 Hi3861 WiFi IoT 开发板，正确的编译命令为 `python build.py wifiiot`（或 `python3 build.py wifiiot`）。

3. **确认系统硬件查询方式**
   - 整理了在 Windows 系统中快速打开“设备管理器”的四种方法（快捷键 `Win+R` 输入 `devmgmt.msc`、右键此电脑、系统搜索、`Win+X` 菜单），用于后续排查开发板串口（COM口）及驱动状态。

---

### 待办事项 / 下一步计划

- [ ] 在终端执行 `python build.py wifiiot`，确认终端输出 `BUILD SUCCESS`。
- [ ] 打开设备管理器，确认开发板对应的 COM 端口号。
- [ ] 按照规范配置 HiBurn 烧录工具，将 `out/wifiiot/` 目录下生成的固件烧录至开发板。
- [ ] 使用串口调试工具（波特率 115200）验证开发板是否正常输出 "Hello World" 等日志。已成功