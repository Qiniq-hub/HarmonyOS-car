嵌入式开发工作日志：解决 C 语言多文件变量重复定义 (L6200E) 报错
日期：2026-09-01
任务目标：解决 Keil 编译时出现的 `L6200E: Symbol multiply defined` 链接报错，实现串口数据与电机控制模块的跨文件数据共享。
当前状态：已定位问题根因，待执行代码修正。

1. 问题描述
在 Keil 环境下进行全量编译（Rebuild）时，链接阶段（Linking）报出以下致命错误，导致无法生成 `.axf` 目标文件：
..\OBJ\Template.axf: Error: L6200E: Symbol uart_rec_flag multiply defined (by control_system.o and usart.o).
..\OBJ\Template.axf: Error: L6200E: Symbol CAR_buff multiply defined (by control_system.o and usart.o).
2. 根因分析
`L6200E` 错误的本质是全局变量被重复分配了内存。在 C 语言的独立编译机制下，跨文件共享变量必须严格区分“定义（Definition）”与“声明（Declaration）”。
经排查，当前工程中存在以下违规操作：
•在 `usart.c` 中正确定义了变量（分配内存）。
•但在 `control_system.c` 或 `usart.h` 中，再次使用了 `u8 uart_rec_flag;` 这种不带 `extern` 关键字的语句，导致编译器试图再次为其分配内存，从而引发冲突。
3. 解决方案与执行标准
为彻底解决此问题，需对以下三个文件进行严格的规范化修改：
3.1 修改 `usart.c`（变量的唯一“家”）
确保变量定义在所有函数外部，且不加 `extern` 关键字。
/* usart.c */
#include "usart.h"
// 【正确】在这里真正分配内存
u8 uart_rec_flag = 0;
u8 CAR_buff[4] = {0};
void USART1_IRQHandler(void) { ... }
3.2 修改 `usart.h`（变量的“名片”）
头文件中只能包含带 `extern` 关键字的声明，严禁直接定义变量。
/* usart.h */
#ifndef __USART_H
#define __USART_H
#include "sys.h"
// 【正确】必须加 extern，仅作为声明
extern u8 uart_rec_flag;
extern u8 CAR_buff[4];
void uart_init(u32 bound);
#endif
3.3 修改 `control_system.c`（变量的“使用者”）
只要包含了 `usart.h`，就绝对不要再写任何关于这两个变量的定义或声明语句。
/* control_system.c */
#include "control_system.h"
#include "usart.h" // 包含头文件即可认识外部变量
// 【正确】此处保持干净，不要写 extern，更不要写 u8 uart_rec_flag;
void System_Control(void) {
// 直接使用变量，无需任何前置声明
if(uart_rec_flag == 1) {
// 解析 CAR_buff ...
}
}
4. 下一步计划
1.按照上述标准修改并保存 `usart.c`、`usart.h`、`control_system.c` 三个文件。
2.执行 Keil 的 `Rebuild` 全量编译，验证 `L6200E` 报错是否消除。
3.编译通过后，进入下一阶段的逻辑验证：通过串口助手发送测试指令，观察 `CAR_buff` 的解析结果以及电机 PID 的响应情况。