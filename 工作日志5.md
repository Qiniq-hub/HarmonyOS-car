# OpenHarmony (Hi3861) 超声波测距模块开发工作日志

**日期**：2026-08-27
**平台**：OpenHarmony / Hi3861 WiFi IoT 套件
**模块**：HC-SR04 超声波测距

## 一、 工作内容概述
基于 OpenHarmony 系统，在 Hi3861 芯片上完成了 HC-SR04 超声波测距功能的代码编写与调试。通过配置 GPIO7 和 GPIO8 引脚，实现了触发信号发送与回波信号捕获，并利用系统微秒级定时器完成了距离计算。

## 二、 核心开发细节

### 1. 环境配置与头文件引入
*   **依赖库**：引入了 `wifiiot_gpio.h`、`hi_io.h`、`hi_time.h` 等核心驱动库，以及 `cmsis_os2.h` 用于多线程任务管理。
*   **看门狗处理**：在任务初始化阶段调用 `WatchDogDisable()` 关闭看门狗，防止因测距等待时间过长导致系统复位。

### 2. 硬件接口定义
*   **引脚分配**：
    *   **GPIO_7**：配置为输出模式 (`WIFI_IOT_GPIO_DIR_OUT`)，连接模块 Trig 端，用于发送触发脉冲。
    *   **GPIO_8**：配置为输入模式 (`WIFI_IOT_GPIO_DIR_IN`)，连接模块 Echo 端，用于接收回响信号。
*   **复用功能**：使用 `hi_io_set_func` 将 GPIO8 设置为普通 GPIO 功能 (Func 0)。

### 3. 测距算法实现 (`GetDistance`)
*   **触发时序**：向 GPIO_7 发送高电平，延时 `hi_udelay(20)`（20us），随后拉低，满足 HC-SR04 至少 10us 的触发要求。
*   **脉宽捕获**：
    *   利用 `while(1)` 循环轮询 GPIO_8 状态。
    *   检测到高电平上升沿时，记录起始时间 `start_time = hi_get_us()`。
    *   检测到低电平下降沿时，计算时间差 `time = hi_get_us() - start_time`。
*   **距离换算**：根据声速公式计算距离（单位 cm）：`distance = time * 0.034 / 2`。

### 4. 任务调度与测试
*   **线程创建**：定义了 `Hcsr04` 入口函数，设置栈大小为 10240，优先级为 `osPriorityNormal`，并通过 `osThreadNew` 创建名为 "Hcsrtext" 的测距子线程。
*   **循环测试**：在子线程中每 2秒 (`osDelay(200)`) 执行一次测距，并通过串口打印 `distance is %.1f (cm)`。
*   **启动入口**：使用 `APP_FEATURE_INIT(Hcsr04)` 宏实现任务的自动注册与启动。

## 三、 关键代码逻辑总结
1.  **GPIO 初始化**：先设置 IO 复用功能，再配置输入/输出方向。
2.  **时序控制**：严格遵循“触发 -> 等待回响 -> 记录高电平持续时间”的逻辑。
3.  **单位转换**：Hi3861 的 `hi_get_us()` 返回微秒值，乘以声速系数 0.034 (cm/us) 后除以 2 (往返路程)，得到单程距离。